describe('UserService', () => {
  describe('get', () => {
    it('returns user by id', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('list', () => {
    it('returns users', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('insert', () => {
    it('insert user on database', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('get', () => {
    it('delete user from database', () => {
      expect(false).toBeTruthy();
    });
  });
});
