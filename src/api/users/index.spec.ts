describe('usersAPI', () => {
  describe('get', () => {
    it('returns users from database by id', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('list', () => {
    it('returns users from database', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('insert', () => {
    it('insert user to database', () => {
      expect(false).toBeTruthy();
    });
  });

  describe('delete', () => {
    it('delete user in database', () => {
      expect(false).toBeTruthy();
    });
  });
});
